= javascript_include_tag "world3d"

%canvas#container

#myModal.modal.fade{"aria-hidden" => "true", "aria-labelledby" => "myModalLabel", role: "dialog", tabindex: "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
        %h4#myModalLabel.modal-title Instruction
      .modal-body
        Use 
        %strong W A S D 
        keys to 
        %strong move
        .br
        Use 
        %strong I J K L
        keys to 
        %strong change view
      .modal-footer
        %button.btn.btn-default{"data-dismiss" => "modal", type: "button"} OK

:coffeescript
  window.name = ""
  while window.name == undefined or window.name == "" or window.name == null or window.name == "null"
    window.name = prompt("Enter your name (maximum 17 characters): ") 
  window.name = window.name.substring(0, 17)
  $('#myModal').modal()
  Application.main()

